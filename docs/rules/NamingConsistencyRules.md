# 명명 일관성 규칙 (Naming Consistency Rules)

## 규칙

- 함수의 이름은 해당 파일의 이름과 정확히 일치해야 합니다.
- 파일 명명 규칙에 따라 코드 파일은 소문자로 시작하므로, 함수명 역시 소문자로 시작하는 `camelCase`를 따르게 됩니다.
- 예: `calculateTotal` 함수는 `calculateTotal.ts` 파일에 위치해야 합니다.

## 목적

- 코드 탐색 및 유지보수 시 직관성을 확보합니다.
- 파일 구조가 곧 프로젝트의 기능 구조를 나타내도록 합니다.
