# 폴더 명명 규칙 (Folder Naming Rules)

## 규칙

- 모든 폴더 이름은 **소문자**로 시작하는 `camelCase` 형식을 따릅니다.
- 예: `rules/`, `specs/`, `core/`, `app/`, `infra/`

## 목적

- 폴더는 논리적 그룹(Namespace)을 나타내며, 대문자로 시작하는 문서 파일과 시각적으로 명확히 구분되도록 합니다.
